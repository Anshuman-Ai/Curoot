name: curoot
description: "Curoot Frontend Application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.4.0
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.1.0
  flutter_riverpod: ^3.3.1
  file_picker: ^11.0.2
  desktop_drop: ^0.7.0
  http: ^1.6.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
